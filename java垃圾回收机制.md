java垃圾回收机制

1.GC通过确定是否被活动对象引用来确定是否收集该对象。

2.触发GC的条件：
    （1）GC在优先级最低的线程中运行，一般在没有线程运行的时候被调用。
	（2）java堆内存不足时，当应用线程在运行时并创建了新的对象，此时如果内存空间不足，JVM会强制调用GC
	     如果一次调用并没有满足内存分配，会调用第二次，如果仍没有满足，会报 out of memory 的错误
